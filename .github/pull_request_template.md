## Summary

<!-- What does this change and why? -->

## Changes

-

## Checklist

- [ ] Backend: `ruff check .` + `ruff format --check .` + `pytest` pass
- [ ] Frontend: `pnpm lint` + `pnpm exec tsc --noEmit` + `pnpm test` pass
- [ ] New/changed user-facing strings are translated (fa + en) and in parity
- [ ] New settings have a default and a line in `.env.example`
- [ ] Tests added/updated for the change
